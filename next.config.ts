import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "www.beev.co",
      "mediaservice.audi.com",
      "images.dealer.com",
      "static-assets.tesla.com",
      "adhgfzvyfq.cloudimg.io",
      "www.pngplay.com",
    ],
  },
};

export default nextConfig;
